name: 'Setup Python with uv'
description: 'Setup Python environment with uv package manager and install dependencies'

inputs:
  python-version:
    description: 'Python version to install'
    required: false
    default: '3.12'
  uv-version:
    description: 'uv version to install (empty = latest)'
    required: false
    default: ''
  install-groups:
    description: 'Dependency groups to install via --group (e.g., "dev", "dev docs")'
    required: false
    default: ''
  install-extras:
    description: 'Optional-dependency extras to install via --extra (e.g., "dev,docs")'
    required: false
    default: ''
  verify-lock:
    description: 'Whether to verify the uv.lock file is up to date'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        version: ${{ inputs.uv-version }}
        enable-cache: true
        cache-dependency-glob: uv.lock

    - name: Verify lock file is up to date
      if: inputs.verify-lock == 'true'
      shell: bash
      run: |
        echo "::group::Verifying uv.lock file is up to date"
        uv lock --check
        echo "::endgroup::"

    - name: Install Dependencies
      shell: bash
      env:
        INSTALL_GROUPS: ${{ inputs.install-groups }}
        INSTALL_EXTRAS: ${{ inputs.install-extras }}
      run: |
        echo "::group::Preparing uv sync command"
        args=("--frozen")

        if [[ -n "$INSTALL_EXTRAS" ]]; then
          echo "Installing extras: $INSTALL_EXTRAS"
          IFS=' ,' read -r -a extras <<< "$INSTALL_EXTRAS"
          for extra in "${extras[@]}"; do
            if [[ -n "$extra" ]]; then
              args+=("--extra" "$extra")
            fi
          done
        fi

        if [[ -n "$INSTALL_GROUPS" ]]; then
          echo "Installing groups: $INSTALL_GROUPS"
          IFS=' ,' read -r -a groups <<< "$INSTALL_GROUPS"
          for group in "${groups[@]}"; do
            if [[ -n "$group" ]]; then
              args+=("--group" "$group")
            fi
          done
        fi

        echo "Running: uv sync ${args[*]}"
        echo "::endgroup::"

        echo "::group::Installing dependencies with uv"
        uv sync "${args[@]}"
        echo "::endgroup::"

    - name: List environment contents
      shell: bash
      run: |
        echo "::group::Installed packages summary"
        uv pip list
        echo "::endgroup::"
