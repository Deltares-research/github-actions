[project]
name = "test-mkdocs-deploy-pixi"
version = "0.1.0"
description = "Fixture for mkdocs-deploy: pixi with mike + mkdocs via pypi-dependencies"
channels = ["conda-forge"]
platforms = ["linux-64", "win-64"]

[dependencies]
python = "3.12.*"

[pypi-dependencies]
mike = "*"
mkdocs = "*"
