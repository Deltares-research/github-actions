[project]
name = "test-mkdocs-deploy-uv"
version = "0.1.0"
description = "Fixture for mkdocs-deploy: uv with mike + mkdocs in the docs group"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
docs = ["mike", "mkdocs"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
