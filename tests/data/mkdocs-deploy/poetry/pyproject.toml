[tool.poetry]
name = "test-mkdocs-deploy-poetry"
version = "0.1.0"
description = "Fixture for mkdocs-deploy: poetry with mike + mkdocs in the docs group"
authors = ["Test <test@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.group.docs.dependencies]
mike = "*"
mkdocs = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
