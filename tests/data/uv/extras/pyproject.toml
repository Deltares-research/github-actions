[project]
name = "test-uv-extras"
version = "0.1.0"
description = "Fixture for python-setup/uv: PEP 621 optional-dependencies (extras)"
requires-python = ">=3.12"
dependencies = ["requests"]

[project.optional-dependencies]
docs = ["sphinx"]
dev = ["pytest"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
