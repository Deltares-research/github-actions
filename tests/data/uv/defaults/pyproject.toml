[project]
name = "test-uv-defaults"
version = "0.1.0"
description = "Fixture for python-setup/uv: minimal project, no groups or extras"
requires-python = ">=3.12"
dependencies = ["requests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true
