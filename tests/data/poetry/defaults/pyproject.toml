[tool.poetry]
name = "test-poetry-defaults"
version = "0.1.0"
description = "Fixture for python-setup/poetry: minimal project, no groups or extras"
authors = ["Test <test@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
