\documentclass{article}
\title{release latex-manual end-to-end test}
\author{CI}
\begin{document}
\maketitle
This document is self-contained so pdflatex can compile it without the
Deltares styles (which are unavailable in CI). It cites a stub
source~\cite{stub2026} so the action's bibtex branch (triggered by the
citation marker in the .aux file) runs end to end.

\bibliographystyle{plain}
\bibliography{refs}
\end{document}
