[project]
name = "test-pip-extras"
version = "0.1.0"
description = "Fixture for python-setup/pip: PEP 621 optional-dependencies (extras)"
requires-python = ">=3.12"
dependencies = ["requests"]

[project.optional-dependencies]
dev = ["pytest"]
docs = ["sphinx"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
