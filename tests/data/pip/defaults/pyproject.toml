[project]
name = "test-pip-defaults"
version = "0.1.0"
description = "Fixture for python-setup/pip: project metadata only (no requirements.txt)"
requires-python = ">=3.12"
dependencies = ["requests"]

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
